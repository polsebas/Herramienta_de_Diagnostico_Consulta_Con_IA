# Dependencias principales para el sistema RAG
agno>=0.1.0
pymilvus>=2.3.0
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.30.0
openai>=1.0.0

# Dependencias adicionales para procesamiento de texto
numpy>=1.24.0
pandas>=2.0.0

# Dependencias para desarrollo
python-dotenv>=1.0.0
requests>=2.31.0

# Dependencias opcionales para mejor rendimiento
accelerate>=0.20.0

# Dependencias para Next Level (PR-1 + PR-2)
tiktoken>=0.5.0
rank-bm25>=0.2.2
fastapi>=0.104.0
uvicorn>=0.24.0
streamlit>=1.28.0

# Dependencias para PR-2 (Context Manager + Logging)
plotly>=5.17.0
openpyxl>=3.1.0

# Dependencias para PR-A (GitHub Indexing)
PyGithub>=2.1.0
PyYAML>=6.0.0

# Dependencias para PR-B (Human-in-the-Loop)
aiohttp>=3.8.0

# Dependencias para PR-D (Cursor Integration)
aiofiles>=23.2.1

# Desarrollo y testing
pylint
pytest>=7.4.0
pytest-asyncio>=0.21.0